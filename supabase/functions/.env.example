# Secrets read by Edge Functions running under `supabase functions serve` locally.
# Copy this to `supabase/functions/.env` (gitignored) and fill in real values.
# For production, set the same keys via `supabase secrets set KEY=value`.

ANTHROPIC_API_KEY=sk-ant-...
