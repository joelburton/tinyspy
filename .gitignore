# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Supabase
supabase/.branches
supabase/.temp
supabase/snippets

# Claude Code per-user settings
.claude/settings.local.json

# Netlify CLI local state
.netlify

# Edge Function secrets
supabase/functions/.env

# Hosted-Supabase import: the runner (import-to-hosted.sh) is committed;
# its secrets + per-deployment values live in import-to-hosted.secrets.sh,
# which is NOT (copy it from import-to-hosted.secrets.example.sh).
import-to-hosted.secrets.sh

# Playwright (e2e smoke tests)
/test-results/
/playwright-report/
/playwright/.cache/

waffle-samples.txt
