# onscreen-keyboard

The on-screen QWERTY for a game whose letters land on the board rather than in an entry box. Its Enter and ⌫ caps are the game's own `actSubmit` / `actDelete` bindings, worn through `actionSurface`, so a cap and its physical key never disagree.
