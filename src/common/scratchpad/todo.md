# scratchpad — todo

## Bugs

## Soon

## Someday

- `GameScratchpadCompanion` is monospace (`ui-monospace, 'SF Mono', …`).
  Plausibly right — it is a notepad — but it is the same unexamined choice as
  the five setup forms (`setup-form/todo.md`), so it gets asked at the same
  time.

## Maybe
