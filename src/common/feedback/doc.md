# feedback

The near-input feedback pill and the local and global state behind it — what a message is, and which slot it lands in. `act-dismiss-feedback` is the any-key watcher that clears the last message without claiming the keystroke.
