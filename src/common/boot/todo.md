# boot — todo

## Bugs

## Soon

- **Four files written after the area closed await Joel's read**:
  `reloadOnStaleBuild.ts` + `.test.ts` and `reloadNotice.ts` + `.test.ts`
  (2026-09-19, the stale-build work). They carry `cs-unmet`; the rest of
  the folder is `cs-blessed-boot`. The same work touched four blessed files
  a line or two each — `main.tsx`, `App.tsx`, `reloadOnStaleChunk.ts`,
  `vite.config.ts` — and `GamePageGate.tsx` and `dbEnvelope.ts` in their own
  areas; stamps unmoved.

## Someday

## Maybe

## Won't do
