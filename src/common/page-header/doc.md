# page-header

The top strip every page carries, and the marks in it. It is no one page's, which is why it lives here rather than under home, club or game. `ChatButton` shows the app-root `act-open-chat` through `useAppAction` (its bubble names the key via `nameWithKey`); `ScratchpadButton` binds `act-open-scratchpad` itself.
