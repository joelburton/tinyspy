# info-sheet

The info column: its mobile sheet, the switch that changes what it shows, and the bordered panel its readouts wear. The switch is `act-toggle-info-sheet`, worn through `actionSurface` so the mark keeps its own look.
