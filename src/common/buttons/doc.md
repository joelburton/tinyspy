# buttons

Every purpose button in the app, and the `StandardButton` they are built from. A button here is a LOOK — an icon, a default name, a tone — and not logic, which is why they live together whoever uses them.
