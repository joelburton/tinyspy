# terminal

What shows when a game ends: the action row, the local terminal row, and the celebration. `terminalMessage` is the verdict's own words, written here because two surfaces have to say the same thing — the pill `common/feedback` draws, and the info column's outcome line.
