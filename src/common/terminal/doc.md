# terminal

What shows when a game ends: the celebration, and the words. `terminalMessage` is the verdict's own words, written here because two surfaces have to say the same thing — the pill `common/feedback` draws, and the line the info column's action row puts beside its buttons (`game-page/InfoActionsRow`, which draws every state of that row, not just the terminal one).
