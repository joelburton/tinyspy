# lists — todo

## Bugs

## Soon

- **Near-miss values, still literal.** `FilterSelect`'s `font-size: 0.8rem`
  (×2) sits just under `--font-size-2`, and its `gap: 0.35rem` and
  `SelectionList`'s `0.6rem` / `0.85rem` gaps fall between spacer steps. Each
  is a decision — move to the step, or say it is bespoke — not a conversion.
  Paddings are parked separately (see `core-css/todo.md`).

## Someday

## Maybe
