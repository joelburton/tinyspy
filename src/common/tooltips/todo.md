# tooltips — todo

## Bugs

## Soon

## Someday

- **Consider non-text content in a bubble.** A tooltip is a string today
  (`data-tooltip`), so an action's key rides inside its words as "New game ·
  +". A styled key cap, or anything richer than text, needs `TooltipHost` to
  take more than a string.

## Maybe
