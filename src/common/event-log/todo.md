# event-log — todo

## Bugs

## Soon

## Someday

## Maybe

- **Should the history banner's ✕ be a `<CloseButton>`?** `HistoryBanner` draws
  its exit as a bare glyph with its own padding; `<CloseButton>` adds a hover
  wash and an `em`-sized box. One file, but it changes the exit in every game
  with a turn viewer, so decide it by eye.

## Won't do
