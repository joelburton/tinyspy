# word-entry

The typed-word box and the row it sits in. There is no `<input>`: the keys are actions — `useCaptureKeys` binds the typing, `useArrowHistory` binds `act-recall-last` / `act-clear-entry` for the arrows — and `<MoveRow>`'s buttons are `<ActionButton>`s placing the same bindings.
