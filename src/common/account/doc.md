# account

Your own menu and profile editing — the modal and the store behind it. The menu's submenu binds `act-edit-profile`, `act-add-word` and `act-log-out`. The color picker it edits with is `fields/ColorChoiceField`.
