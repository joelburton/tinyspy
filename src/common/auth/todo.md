# auth — todo

## Bugs

## Soon

## Someday

- `ClaimHandleScreen.module.css`'s `.buttonRow` and
  `floating-panels/modalActions.module.css` draw the same row — right-aligned
  flex, a `6rem` button floor, the same gap. They differ only in the space
  above (0.5rem here, `--spacer-1` there) and in the shared class being named
  for modals while this is a page. One rule when floating-panels is open.

## Maybe
